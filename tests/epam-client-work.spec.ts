import { test, expect } from '@playwright/test';

test.describe('EPAM Client Work navigation', () => {
  test('Navigate to Client Work from Services', async ({ page }) => {
    // Test steps will be added in a follow-up update
  });
});
